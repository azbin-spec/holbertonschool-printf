#include <stdio.h>

int printIntegers() {
    int D = 10;  // Initialize D
    int I = 20;  // Initialize I
    printf("D = %d, I = %d\n", D, I);
    return 0;  // Return 0 to indicate successful execution
}

int main() {
    return printIntegers();  // Call printIntegers() and return its result
}


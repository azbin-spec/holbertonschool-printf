.TH _PRINTF 3 "November 2024" "1.0" "Library Functions"
.SH NAME
_printf \- produces output according to a format
.SH SYNOPSIS
.B #include "main.h"
.PP
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The \fB_printf\fP function produces output according to a specified format string and a variable number of arguments. It works similarly to the standard \fBprintf\fP function in C, supporting various format specifiers.
The function supports the following format specifiers:
.TP
.B %c
Print a single character.
.TP
.B %s
Print a string.
.TP
.B %%
Print a literal percent sign.
.TP
.B %d, %i
Print an integer.
The function scans the \fBformat\fP string character by character and produces the corresponding output based on the format specifiers. For unsupported specifiers or an invalid format, the function prints a literal percent sign followed by the character that follows the percent symbol.
.SH RETURN VALUE
The function returns the number of characters printed (not including the terminating null byte) or \-1 if there is an error, such as when \fBformat\fP is NULL.
.SH EXAMPLES
.TP
printf("Hello, %s!", "world");
Prints: Hello, world!
.TP
_printf("Character: %c", 'A');
Prints: Character: A
.TP
_printf("Percent sign: %%");
Prints: Percent sign: %
.SH SEE ALSO
.BR printf (3)
